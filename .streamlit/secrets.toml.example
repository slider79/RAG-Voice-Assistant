# TEMPLATE ONLY - for the optional standalone Streamlit client (app.py).
# Copy to .streamlit/secrets.toml (gitignored) for local use.
#
# The Streamlit client is a thin front-end over the deployed backend, so it
# only needs the backend URL (and the secret, if you set one).

BACKEND_URL = "https://your-backend.vercel.app"
# BACKEND_SECRET = "the-same-value-if-you-set-one"
