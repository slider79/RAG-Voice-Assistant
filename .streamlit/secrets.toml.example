# TEMPLATE ONLY - do not put real keys here. Copy to .streamlit/secrets.toml
# (gitignored) for local use, or paste into Streamlit Cloud -> App settings -> Secrets.

GROQ_API_KEY = "gsk_your_key_here"
GEMINI_API_KEY = "your_gemini_key_here"

# Only needed if you run the FastAPI backend from Streamlit's environment.
# RAG_STORE_DIR = "/tmp/rag_store"
